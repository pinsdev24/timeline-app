/**
 * Authentication Service Entry Point
 */
require('dotenv').config();
const server = require('./src/server');

// This file is kept minimal and just serves as the entry point
// All server setup logic is in src/server.js
